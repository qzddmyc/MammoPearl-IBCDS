o 将logs按userName分类
o 将用户名加入模版
o 登录页面 + 登录、登出按钮变化。使用localStorage.getItem('usr');存取
- 技术优势页面
- docker打包
o 封装Admin-Operation为二进制文件
o 集成ai功能至页面
x 优化数据库服务未开启时的超长延时 --无法实现
- ai-qa-container & qa-container 在平板宽度时，右侧贴合 scrollbar 过紧的样式问题
- logs.results路径已更新，同步至README (?)
o json数据存储工作
o 对于AI工作，注意未等待AI回复就被强制中断的情况
- ai页面添加后端是否开启的确认
o INIT_check_if_json_available添加进app.run()之前
- 添加AI页面的渲染
o python对同一文件同时读写时可能存在的问题
o 写入json时报错则发送post请求至ai页面
- 存在未执行操作时，禁止二次问答
- /api/get_full_json的链式调用中，不需要判断unresolved。或者说，在初始化调用时，不需要使用该参数（暂不修改）
- 初始化AI页面时，若存在unresolved事件，则禁止用户继续添加。用户添加问答之后同理。封装成函数
- 添加：回车键允许提交文本域的内容